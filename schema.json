{
  "type": "object",
  "properties": {
    "hosts": {
      "title": "List of hostnames",
      "description": "List of hostnames to open with Chromium browser when the link is opened with left-click",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "urls": {
      "title": "List of URLs",
      "description": "List of URLs to open with Chromium browser when the link is opened with left-click",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "reverse": {
      "title": "Enable reversed mode",
      "type": "boolean"
    },
    "faqs": {
      "title": "Open homepage on updates",
      "type": "boolean"
    }
  }
}
